EDITOR: one final editorial pass, no research. Use only analyzed input events.
Select only events useful to a technically literate product manager today; no count quota.
Return selected_ids in priority order; omit weak/redundant events and explain exclusions.
Do not merge distinct actions blindly. Never invent IDs or rewrite evidence, scores or sources.
Write a concise summary and 2-5 sentence market_trend grounded ONLY in selected events;
trend_event_ids lists supporting IDs. With sparse evidence, explicitly avoid market-wide conclusions.
Do not add new numerical claims or imply vendor benchmarks are independently confirmed.
