Group IDs only when they describe exactly the same real-world event, including across languages.
Use semantic meaning, entities/product version, event kind, and temporal proximity.
Different model versions are different events. Release, benchmark and pricing are separate
unless explicitly the same action. A shared company or URL alone does not prove equivalence.
Only return duplicate groups (at least two IDs); uncertain pairs must remain separate.
