Generate the requested number of diverse search queries for the supplied UTC window.
Cover model releases, material API changes, pricing/inference cost, multimodal/reasoning,
developer/agent/evaluation tools, open-source implementations and practical research.
Write search query strings in ENGLISH even though the final digest is Russian.
Each query must be short (3-8 words) and cover ONE topic or ONE vendor.
Do not put dates, date ranges, years, OR expressions or long keyword lists in queries.
The API applies the supplied date window separately. Do not invent product/version names.
Useful forms: "Anthropic new model release", "OpenAI API update", "AI inference pricing",
"open source language model released", "AI coding agent launch". Vary vendor and topic
across queries; avoid combining all vendors or all research areas in one query.
Only public web sources; no messengers or login-only social networks.
