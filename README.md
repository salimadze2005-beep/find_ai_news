# AI Intelligence

Evidence-first AI industry digest for the last 48 hours. Development in progress;
see PROJECT_STATUS.md and TODO.md for verified completion.

Python 3.11+:
```sh
python -m venv .venv
# Windows: .venv\Scripts\activate
# Linux/macOS: source .venv/bin/activate
pip install -r requirements.txt
python -m app.run
python -m pytest
```
